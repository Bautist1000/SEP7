@page "/"

<PageTitle>Home</PageTitle>

<div class="home-container">
    <h1>Welcome to AquAnalyzer!</h1>

    <p>Your ultimate tool for tracking water sustainability! Designed for Kamstrup, AquAnalyzer helps you monitor water usage and improve ESG performance with ease. Dive into data, visualize metrics, and make every drop count!</p>

    <div class="image-container">
        <img src="images/AquAnalyzerPersonPic.png" alt="PersonPic" class="image">
        <img src="images/AquAnalyzerNaturePic.png" alt="NaturePic" class="image">
    </div>
</div>

<style>
    .home-container {
        background-color: white; /* White background */
        padding: 20px; /* Padding around the content */
        border-radius: 8px; /* Rounded corners */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    }

    h1 {
        color: #2c3e50; /* Dark blue color */
        margin-bottom: 16px; /* Space below the title */
    }

    p {
        color: #34495e; /* Slightly lighter blue */
        margin-bottom: 24px; /* Space below the paragraph */
        padding: 16px; /* Padding inside the paragraph */
        border-radius: 8px; /* Rounded corners */
        background-color: #f0f8ff; /* Light blue background */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    }

    .image-container {
        display: flex;
        justify-content: space-around;
        align-items: center;
        margin-top: 24px; /* Space above the images */
    }

    .image {
        width: 300px; /* Fixed width */
        height: 200px; /* Fixed height */
        object-fit: contain; /* Ensure the entire image is visible without distortion */
        border-radius: 8px; /* Rounded corners */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    }
</style>
